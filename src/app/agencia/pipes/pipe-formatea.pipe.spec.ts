import { PipeFormateaPipe } from './pipe-formatea.pipe';

describe('PipeFormateaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFormateaPipe();
    expect(pipe).toBeTruthy();
  });
});
